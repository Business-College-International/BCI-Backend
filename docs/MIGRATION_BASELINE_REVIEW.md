# Initial migration baseline review

This file exists solely to trigger the repository's migration-review workflow against the current authoritative Prisma schema.

The generated SQL artifact is review-only until it is committed into `prisma/migrations` as the canonical initial migration after schema/invariant review.
